@model Assignment.Models.Bill

@{
    //ViewData["Title"] = "EditBill";
}

<h1>EditBill</h1>

<h4>Bill</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form  method="post" action="~/Home/EditBill">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @Html.HiddenFor(m => m.ID)
            @Html.HiddenFor(m => m.CreateDate)
            @Html.HiddenFor(m => m.UserId)
         
            <div class="form-group">
                <label asp-for="Status" class="control-label"></label>
                <select class="form-select" aria-label="Default select example" asp-for="Status">                  
                    <option value="1">da thanh taon</option>
                    <option value="2">chua thanh toan</option>
                    
                </select>
                <span asp-validation-for="Status" class="text-danger"></span>
            </div>
            <div class="form-group">
                <button type="submit" class="col-5 btn rounded my-2">Chỉnh sửa</button>
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
